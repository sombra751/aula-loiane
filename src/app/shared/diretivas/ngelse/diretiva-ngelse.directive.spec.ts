import { DiretivaNgelseDirective } from './diretiva-ngelse.directive';

describe('DiretivaNgelseDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaNgelseDirective();
    expect(directive).toBeTruthy();
  });
});
